ui <- bs4DashPage(
  title       = tit_app,
  freshTheme  = NULL,
  dark        = NULL,
  scrollToTop = TRUE,
  fullscreen  = FALSE,
  help        = NULL,
  preloader   = preloader,
  header      = header,
  sidebar     = sidebar,
  body        = body,
  controlbar  = controlbar,
  footer      = footer
)
