# UI ---------------------------------------------------------------------------
# Define la estructura visual principal de la app con componentes bs4Dash.

ui <- bs4DashPage(
  title = tit_app,
  header = header,
  sidebar = sidebar,
  body = body,
  controlbar = controlbar,
  footer = footer
)
